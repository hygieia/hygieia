/**
 * Created by hyw912 on 4/10/16.
 */

/*

describe('featureViewController', function () {

    var scope;
    var featureDataMock;
    var featureViewController;

    // load the controller's module
    beforeEach(module(HygieiaConfig.module));

    // define the mock Parse service
    beforeEach(function() {

        featureDataMock = {

            registerBook: function(book) {


            },
            getBookRegistrationId: function() {

            }

        };
    });
    // inject the required services and instantiate the controller
    beforeEach(inject(function($rootScope, $controller) {
        scope = $rootScope.$new();

        scope.widgetConfig = { options: {
            teamId: 123,
            teamName: "bogus"
        }};

        featureViewController = $controller('featureViewController', {
            $scope: scope, featureData: featureDataMock
        });
    }));

    it('should instantiate the controller correctly and run', function() {
        expect(featureViewController).not.toBeUndefined();
    });

});
*/
