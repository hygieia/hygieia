import { TemplatesDirective } from './templates.directive';

describe('TemplatesDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplatesDirective(null);
    expect(directive).toBeTruthy();
  });
});
